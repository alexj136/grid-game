package GameLogic;

/**
 * An EndFloorTile is the same as a FloorTile but also is a valid finishing
 * location.
 * @author Alex Jeffery
 * @version 0.1
 */
public class EndFloorTile extends FloorTile {

    /**
     * Construct a new EndFloorTile, which will not have been visited.
     * @return an unvisited EndFloorTile
     */
    public EndFloorTile() {
        super();
    }
}
